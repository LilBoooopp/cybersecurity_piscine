services:
  onion:
    container_name: onion_web
    build:
      context: ./onion/
      dockerfile: Dockerfile
    image: onion_web
    pull_policy: never
    volumes:
      - onion:/var/lib/tor/hidden_service/
    restart: always

volumes:
  onion:

